1. 遍历某个目录，统计文件数量、目录数量、目录占用空间。
2. 读取employees.txt文件，将雇员信息按入职日期降序排序。
3. 读取employees.txt文件，输出没有担任领导职位的员工信息，按姓名降序排序。
4. 读取employees.txt文件，输出没有担任领导职位的员工信息，按姓名降序排序写到excel表中。
5. 在SQLite数据库中建个雇员表，读取emps.xls文件将雇员信息导入到数据库的表中。
